/** The «Живые проекты» layouts on trial — `?v=1…5` on /portfolio. A plain
 *  module: the server page reads it too, and a value exported from a
 *  "use client" file is only a client reference there. */
export const LIVE_VARIANTS = ["Сплит", "Сетка", "Кино", "Стопка", "Витрина"] as const;
